# OpenAI-compatible chat model. Works with OpenAI, Azure OpenAI, Ollama,
# vLLM, OpenRouter, or any other gateway that implements the spec.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# Optional: point at a self-hosted or alternative gateway.
# OPENAI_BASE_URL=https://api.openai.com/v1
